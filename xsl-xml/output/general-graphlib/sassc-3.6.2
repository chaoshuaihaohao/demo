#!/bin/bash

#Start scripts
tar -xf ../libsass-3.6.5.tar.gz &&
pushd libsass-3.6.5 &&

autoreconf -fi &&

./configure --prefix=/usr --disable-static &&
make
make install
popd &&
autoreconf -fi &&

./configure --prefix=/usr &&
make
make install

#End scripts
