#!/bin/bash

#Start scripts
./configure --prefix=/usr          \
            --shared-cares         \
            --shared-libuv         \
            --shared-openssl       \
            --shared-nghttp2       \
            --shared-zlib          \
            --with-intl=system-icu &&
make
make install &&
ln -sf node /usr/share/doc/node-18.17.1

#End scripts
