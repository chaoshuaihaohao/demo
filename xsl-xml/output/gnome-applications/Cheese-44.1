#!/bin/bash

#Start scripts
mkdir build &&
cd    build &&

meson setup --prefix=/usr        \
            --buildtype=release  \
            -Dgtk_doc=false      \
            -Dtests=true         \
            ..                   &&
ninja
ninja install

#End scripts
