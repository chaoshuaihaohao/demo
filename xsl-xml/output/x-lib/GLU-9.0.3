#!/bin/bash

#Start scripts
mkdir build &&
cd    build &&

meson setup ..              \
      --prefix=$XORG_PREFIX \
      -Dgl_provider=gl      \
      --buildtype=release   &&
ninja
ninja install &&
rm -vf /usr/lib/libGLU.a

#End scripts
